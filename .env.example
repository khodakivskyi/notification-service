# ============================================
# Environment Configuration
# ============================================

# Node.js environment: development, production, test
NODE_ENV=development

# Application port
PORT=3001

# Logging level: error, warn, info, debug
LOG_LEVEL=info

# ============================================
# Database Configuration
# ============================================

# Database user (used by PostgreSQL container)
DB_USER=postgres

# Database password (used by PostgreSQL container)
DB_PASSWORD=postgres

# Database name (used by PostgreSQL container)
DB_NAME=notifications

# Database port (host port mapping)
DB_PORT=5432

# Database connection URL
# For Docker: use 'db' as hostname (service name)
# For Localhost: use 'localhost' as hostname
# Format: postgresql://USER:PASSWORD@HOST:PORT/DB_NAME?sslmode=disable
DATABASE_URL=postgresql://postgres:postgres@db:5432/notifications?sslmode=disable

# ============================================
# SMTP Configuration (Email Service)
# ============================================

# SMTP server hostname
# Examples: smtp.gmail.com, smtp.mailtrap.io, smtp.sendgrid.net
SMTP_HOST=smtp.gmail.com

# SMTP server port
# Common ports: 587 (TLS), 465 (SSL), 25 (unsecured)
SMTP_PORT=587

# SMTP authentication email (sender address)
SMTP_USER=your-email@gmail.com

# SMTP authentication password or app password
# For Gmail: use App Password (!not your regular password)
# Generate at: https://myaccount.google.com/apppasswords
SMTP_PASS=your-app-password

# ============================================
# Docker Configuration (Optional)
# ============================================

# Application port mapping for Docker
# Format: HOST_PORT:CONTAINER_PORT
APP_PORT=3001

# ============================================
# RabbitMQ Configuration
# ============================================

# RabbitMQ connection URL
# For Docker: use 'rabbitmq' as hostname (service name)
# For Localhost: use 'localhost' as hostname
# Format: amqp://USER:PASSWORD@HOST:PORT/VHOST
RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672

# Queue names
EMAIL_QUEUE_NAME=email_notifications

# Dead-lettering (DLQ/DLX)
# Exchange that receives dead-lettered messages from queues
RABBITMQ_DLX_EXCHANGE=notification.dlx
# Dead Letter Queue name for email jobs
EMAIL_DLQ_NAME=email.dlq
# Routing key used to route dead-lettered messages into EMAIL_DLQ_NAME
EMAIL_DLQ_ROUTING_KEY=email.dlq